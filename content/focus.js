/* The focus checklist — what to learn and how far, one row per topic.
   Built in the same pass as the bank's topic tagging (estate rule 2026-09-17: every
   Paper Sim ships it). Empty until that pass is done; the home screen says so. */
export const FOCUS = [];
